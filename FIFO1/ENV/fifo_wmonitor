class fifo_wrmonitor extends uvm_monitor;
`uvm_component_utils(fifo_monitor)

function new (string name "fifo_monitor",uvm_component_parent=null);
super.new(name,parent);
endfunction

task run_phase();
forever
begin

end 
endtask

task send_to_dut();
@(vif.wdr_cb); begin
  vif.wdr_cb.wr_enb<=req.wr_enb;
  vif.wdr_cb.wr_data<=req.wr_data;
  vif.wdr_cd,wr_addr<=req.wr_addr;
  end
endtask